apiVersion: v1
kind: Service

metadata:
    name:   sjsump-frontend-server
    namespace:  sjsump

spec:
    type: NodePort
    selector:
        app:   sjsump-frontend-server
    ports:
        -   port: 3000
            targetPort: 3000
            nodePort: 30008
            name: sjsump-frontend-port
